### App to sharing expenses with friends

#### Project execution
to execute this app only need:
- Docker and docker-compose installed

execute the following commando to start the servers:

`$ docker-compose up`